Levantar docker

docker compose up --build -d

bajarlo

docker compose down

ya lo construiste

docker compose up -d

Pruebas de integración al fusionar un pull request en `main`:
[configuración de Postman y Newman](tests/integration/postman/README.md).

Informe paso a paso y resultados locales:
[pruebas de integración](docs/informe_pruebas_integracion.md).
